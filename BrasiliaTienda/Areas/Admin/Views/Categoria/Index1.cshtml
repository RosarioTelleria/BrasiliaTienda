@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<br />
<br />

<div class="border">
        <div class=" row">
            <div class="col-6">
                <h2 class="text-info">Lista de Usuarios</h2>
            </div>

            <div class="col-6">
            <a asp-area="Identity" asp-page="/Account/Register" class="btn btn-info">
                <i class="fas fa-plus"></i> &nbsp; Registrar  Nuevo Usuario

            </a>
            </div>

        </div>


        <br />
      @*  @if(Model.Count() > 0)
        {
            <table class="table table-striped border">
                <tr class="table-secondary">
                    <th>@Html.DisplayNameFor(m => m.Nombre)</th>
                    <th>@Html.DisplayNameFor(m => m.Telefono)</th>
                    <th>@Html.DisplayNameFor(m => m.Direccion)</th>
                </tr>
        @foreach(var item in Model)
            {
                <tr>
                    <td>@Html.DisplayFor(m => item.Nombre)</td>
                    <td>@Html.DisplayFor(m => item.Telefono)</td>
                    <td>@Html.DisplayFor(m => item.Direccion)</td>

        @if(item.lockoutEnd==null || item.lockoutEnd < DateTime.Now)
            {
             <a class="btn btn-success text-white" asp-action="Bloquear" style="width=50px;" asp-route-id="@item.IdUsuario">
             <i class="fas fa-lock-open"></i>  
             </a>
            }
          else
            {
               <a class="btn btn-danger text-white" asp-action="Desbloquear" style="width=50px;" asp-route-id="@item.IdUsuario">
               <i class="fas fa-lock-lock" ></i> 
               </a>         
            }
                </tr>
            }

            </table>
        
        }
     }*@
</div>
